import type { AvatarInputLabels } from "../types";

/** Defaults en inglés: la app que monta la librería pasa los suyos (L8). */
export const DEFAULT_LABELS: AvatarInputLabels = {
  optional: "(optional)",
  add: "Add photo",
  change: "Change photo",
  remove: "Remove photo",
  previewAlt: "Avatar preview",
  invalidType: "Please choose a valid image file",
  tooLarge: "The image must be smaller than 2MB",
};
